create table student( student_id number primary key, student_name varchar2(30), address varchar2(40), city varchar2(30), department_id number, foreign key(department_id) references department(department_id));
